import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

class Overview2024 extends StatelessWidget {
  const Overview2024({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(
        children: [
          Padding(
          padding: const EdgeInsets.only(top: 50),
          child: Card(
            color: Color(0xff575656FF),
          elevation: 8.0,
          margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
          child: ListTile(
            contentPadding:
            const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
            leading: const Icon(Icons.account_balance, color: Colors.white),
            title: Text("Website Development",style: TextStyle(color: Colors.white),),
            trailing: const Icon(Icons.arrow_forward, color: Colors.white),
            onTap: () {
              // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
            },
          ),
                ),
        ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Mobile App Development",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Machine Learning",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Artificial Intelligence",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Database Projects",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Internet of Things",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Software Development Tools",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Blockchain",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Game Development",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Robotics Projects",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Cloud Computing",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
          SizedBox(height: 16,),
          Card(
            // color: Colors.blue,
            color: Color(0xff575656FF),
            elevation: 8.0,
            margin: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 6.0),
            child: ListTile(
              contentPadding:
              const EdgeInsets.symmetric(horizontal: 20.0, vertical: 10.0),
              leading: const Icon(Icons.account_balance, color: Colors.white),
              title: Text("Software Testing",style: TextStyle(color: Colors.white),),
              trailing: const Icon(Icons.arrow_forward, color: Colors.white),
              onTap: () {
                // Navigator.push(context, MaterialPageRoute(builder: (context)=>Screen()));
              },
            ),
          ),
        ],
      ),
    );
  }
}
