// Define heroCard as a BoxDecoration
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

const heroCard = BoxDecoration(
  color: Colors.white,

  boxShadow: [
    BoxShadow(
      spreadRadius: 2,
      blurRadius: 5,
      offset: const Offset(0, 3),
    ),
  ],
);


const heroNav = BoxDecoration(
  color: Colors.white,

  boxShadow: [
    BoxShadow(
      spreadRadius: 2,
      blurRadius: 5,
      offset: const Offset(0, 3),
    ),
  ],
);
